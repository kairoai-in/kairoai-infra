# Backend Bootstrap

This directory is reserved for the final backend-bootstrap root that creates or validates Terraform state storage for:

- `hubtfstate`
- `testtfstate`
- `prodtfstate`

The current working bootstrap root remains in `../../bootstrap` until we migrate state-management code safely.
