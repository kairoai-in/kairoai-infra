# Test Pipeline Smoke

This file is a harmless marker used to validate the `azure/*` to `test` pull request flow.

It should not be labeled with `apply-test` unless we intentionally want to exercise the Terraform apply gate.
