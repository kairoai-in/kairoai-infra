# Azure Database for PostgreSQL Flexible Server will be defined here.
#
# Keep it in Terraform rather than deploying PostgreSQL as a pod in AKS.
# The first implementation should add networking, private access, backups,
# administrator credentials, and database outputs intentionally.
