# RabbitMQ is the broker for Celery-based async work.
#
# The initial deployment may run RabbitMQ inside AKS via Helm. If we choose a
# managed RabbitMQ-compatible broker later, infrastructure resources can be
# added here without changing application task contracts.
